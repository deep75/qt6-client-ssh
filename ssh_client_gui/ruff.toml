line-length = 88

[lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "N", "C4"]

[lint.per-file-ignores]
# Les membres exposés à QML suivent la convention camelCase de Qt, pas snake_case.
"bridge.py" = ["N802", "N815"]
